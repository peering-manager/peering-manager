---
version: 2
build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  commands:
  - pip install uv
  - uv sync --locked --only-group docs
  - uv run --no-sync zensical build --config-file mkdocs.yaml --strict
  - mkdir -p $READTHEDOCS_OUTPUT/html/
  - cp -r site/. $READTHEDOCS_OUTPUT/html/
